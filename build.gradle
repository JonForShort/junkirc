buildscript {
    repositories {
        jcenter()
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.9'
}

allprojects {
    repositories {
        jcenter()
    }
}

ext {
    JAVA_SOURCE_COMPATIBILITY = JavaVersion.VERSION_1_8
}